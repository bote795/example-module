/* eslint-disable no-console */
const defaultAwesomeFunction = require("../dist");

const defaultVal = defaultAwesomeFunction("Nick");

// defaultVal === 'I am the Default Awesome Function, fellow comrade! - Nick'
console.log(defaultVal);
